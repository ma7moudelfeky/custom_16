<odoo>
    <data>
        <record id="user_can_see_own_customer_rule" model="ir.rule">
            <field name="name">res.partner.user.customers</field>
            <field name="model_id" ref="account.model_res_partner"/>
            <field name="domain_force">['|', ('allowed_user_ids', '=', user.id), ('allowed_user_ids', '=', False)]
            </field>
            <field name="groups" eval="[(4, ref('customer_security.group_customers_user'))]"/>
        </record>

        <record id="admin_can_see_own_customer_rule" model="ir.rule">
            <field name="name">res.partner.admin.customers</field>
            <field name="model_id" ref="account.model_res_partner"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('customer_security.group_customers_admin'))]"/>
        </record>

        <record id="account.account_move_rule_group_invoice" model="ir.rule">
            <field name="name">Readonly Move</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="domain_force">[('partner_id', 'in', user.allowed_partner_ids.ids)]</field>
            <field name="groups" eval="[(4, ref('account.group_account_invoice'))]"/>
        </record>

        <record id="account.account_move_see_all" model="ir.rule">
            <field name="name">All Journal Entries</field>
            <field ref="account.model_account_move" name="model_id"/>
            <field name="domain_force">[('partner_id', 'in', user.allowed_partner_ids.ids)]</field>
            <field name="groups" eval="[(4, ref('account.group_account_invoice'))]"/>
        </record>

<!--        <record id="user_can_see_own_invoices_rule" model="ir.rule">-->
<!--            <field name="name">account.move.user.invoices</field>-->
<!--            <field name="model_id" ref="account.model_account_move"/>-->
<!--            <field name="domain_force">['|', ('partner_id', 'in', user.allowed_partner_ids.ids),-->
<!--                ('partner_id.allowed_user_ids', '=', False)]-->
<!--            </field>-->
<!--            <field name="groups" eval="[(4, ref('customer_security.group_invoices_user'))]"/>-->
<!--        </record>-->

<!--        <record id="admin_can_see_own_invoices_rule" model="ir.rule">-->
<!--            <field name="name">account.move.admin.invoices</field>-->
<!--            <field name="model_id" ref="account.model_account_move"/>-->
<!--            <field name="domain_force">[(1, '=', 1)]</field>-->
<!--            <field name="groups" eval="[(4, ref('customer_security.group_invoices_admin'))]"/>-->
<!--        </record>-->

    </data>
</odoo>